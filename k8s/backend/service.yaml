apiVersion: v1
kind: Service
metadata:
  namespace: three-tier
  name: backend-svc
spec:
  selector:
    app: backend
  ports:
  - name: backend-svc
    protocol: TCP
    port: 3500
    targetPort: 3500
  type: ClusterIP